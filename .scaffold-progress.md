# Scaffold Progress

## Milestone 1: Project Init + Foundation Packages
**Status:** Complete

### What was done
- Initialized Go module (`go 1.23.0`, `toolchain go1.25.9`)
- Created project structure: `cmd/server/`, `internal/database/`, `internal/server/`, `pkg/config/`, `pkg/logging/`, `pkg/errors/`, `migrations/`
- Added `.gitignore`, `.env.example`, `docker-compose.yml` (MongoDB 7), `.golangci.yml`

### pkg/errors (sentinel errors + wrapping)
- **File:** `pkg/errors/errors.go`
- **Exported sentinels:** `ErrNotFound`, `ErrUnauthorized`, `ErrForbidden`, `ErrConflict`, `ErrAlreadyExists`, `ErrInvalidInput`, `ErrInternal`, `ErrUnavailable`
- **Functions:** `Wrap(err, msg)`, `Wrapf(err, fmt, args...)` -- both nil-safe
- **Pattern:** `fmt.Errorf("context: %w", err)` wrapping, checked via standard `errors.Is`/`errors.As`
- **Note:** Standard library `errors.Is`/`errors.As`/`errors.New` are used directly (no re-exports)

### pkg/logging (Zap structured JSON)
- **File:** `pkg/logging/logging.go`
- **Exported:** `NewLogger(level string) *zap.Logger`, `NewDevelopmentLogger() *zap.Logger`
- **Context API:** `WithContext(ctx, logger) context.Context`, `FromContext(ctx) *zap.Logger`, `WithFields(ctx, ...zap.Field) context.Context`
- **Contract:** `FromContext` never returns nil (returns nop logger if none set)

### pkg/config (Viper config loading)
- **File:** `pkg/config/config.go`
- **Exported types:** `Config`, `ServerConfig`, `MongoDBConfig`, `LoggingConfig`, `ShutdownConfig`
- **Functions:** `Load(configPath string) *Config`, `LoadFromEnv() *Config`
- **Env prefix:** `ZEE6DO_` with explicit bindings (e.g. `ZEE6DO_MONGODB_URI`, `ZEE6DO_SERVER_GRPC_PORT`)
- **Validation:** Panics on missing `mongodb.uri`, invalid port (1-65535), invalid environment
- **Defaults:** port=50051, env=development, db=zee6do, pool=100, connect_timeout=10s, timeout=30s, server_selection_timeout=5s, log_level=info, grace_period=30s, drain_interval=10s

## Milestone 2: Database + Server Lifecycle
**Status:** Complete

### internal/database (MongoDB connection + migrations)
- **Files:** `internal/database/mongodb.go`, `internal/database/migration.go`, `internal/database/wire.go`
- **MongoClient exported API:** `NewMongoClient(ctx, *config.Config, *zap.Logger) (*MongoClient, error)`, `Database() *mongo.Database`, `Client() *mongo.Client`, `HealthCheck(ctx) error`, `Close(ctx) error`
- **Pool config:** MaxPoolSize, ConnectTimeout, Timeout (v2 client-level), ServerSelectionTimeout, RetryWrites, RetryReads
- **MongoClient implements** `server.Shutdownable` interface (compile-time checked)
- **MigrationRunner API:** `NewMigrationRunner(db, logger)`, `Register(Migration)`, `Run(ctx) error`
- **Migration struct:** `{ID string, Description string, Up MigrationFunc}` where `MigrationFunc = func(ctx, *mongo.Database) error`
- **Tracking:** `_migrations` collection, records `{_id, description, applied_at}`, sorted by ID, skips already-applied
- **Concurrent safety:** `markApplied()` handles duplicate-key errors gracefully for concurrent startup (ECS rolling deploys)
- **Wire:** `database.ProviderSet` provides `NewMongoClient`

### internal/server (health + shutdown)
- **Files:** `internal/server/health.go`, `internal/server/shutdown.go`, `internal/server/wire.go`
- **HealthChecker API:** `NewHealthChecker() *HealthChecker`, `IsHealthy() bool`, `SetUnhealthy()`
- **Shutdownable interface:** `Close(ctx context.Context) error` -- implement this to register for shutdown
- **ShutdownManager API:** `NewShutdownManager(logger, health, drainInterval, gracePeriod)`, `Register(name, Shutdownable)`, `Shutdown()`
- **Shutdown sequence:** unhealthy -> drain wait -> close components in order -> done (30s total timeout)
- **Bug fix:** Components always close even when grace period expires during drain (no early return)
- **Wire:** `server.ProviderSet` provides `NewHealthChecker`

### migrations
- **File:** `migrations/migrations.go`
- **Function:** `Register(runner *database.MigrationRunner)` -- registers all migrations
- **Initial migration:** `001_create_initial_indexes` -- users unique index on user_id, tasks compound index on (user_id, status, priority)

### cmd/server (entry point + Wire DI)
- **Files:** `cmd/server/main.go`, `cmd/server/app.go`, `cmd/server/wire.go`, `cmd/server/wire_gen.go`
- **App struct:** holds Config, Logger, MongoClient, HealthChecker, ShutdownManager
- **Wire injector:** `InitializeApp(ctx, *config.Config) (*App, error)` -- wires logger, mongo, health, app
- **main.go flow:** parse flags -> load config -> InitializeApp -> run migrations -> wait for SIGINT/SIGTERM -> graceful shutdown
- **Logger selection:** development env -> dev logger, otherwise production JSON logger

### Config / DevOps
- **golangci-lint:** v2 config — govet, errcheck, staticcheck are default-enabled; gosec, revive, exhaustive, gocritic explicitly enabled (all clean)
- **docker-compose.yml:** MongoDB 7 on port 27017, healthcheck, named volume
- **.env.example:** all ZEE6DO_* env vars documented

## Milestone 3: Quality Loop + Testing
**Status:** Complete

### Changes from review
- Migration runner: duplicate-key handling for concurrent startup safety
- Removed low-value error re-exports (Is/As/New) -- use standard library directly
- Simplified config validation (direct panic, no slice accumulation)
- Removed no-op `zap.AddCallerSkip(0)`
- Removed deprecated docker-compose `version` key
- Cleaned up test mocks

### Testing bug fix
- **Shutdown bug found and fixed:** `Shutdown()` had an early `return` when grace period expired during drain, leaking all registered components (MongoDB connections). Fixed to always fall through to component closing.

### Test coverage
- 33 unit test functions, 47 test cases including subtests (race detector clean)
- 5 integration test functions with live MongoDB (race detector clean, build-tagged `//go:build integration`)
- golangci-lint v2 clean (zero findings)
- Integration tests: `internal/database/integration_test.go`, `migrations/migrations_integration_test.go`
- Regression tests: `internal/server/shutdown_grace_bug_test.go`, `internal/server/expired_ctx_test.go`
- Duration env parsing test: `pkg/config/duration_env_test.go`
- Coverage: `internal/server` 100%, `pkg/errors` 100%, `pkg/config` 94.1%, `pkg/logging` 88.9%, `internal/database` 3.2% (requires MongoDB for integration tests)

## Milestone 4: Verification & Gap-Filling Audit
**Status:** Complete

### What was verified
- Line-by-line comparison of every `.scaffold-progress.md` claim against actual code
- All 8 sentinel errors present and distinct (tested)
- All config defaults verified (10 defaults match documentation)
- All 11 env var bindings verified present in config and `.env.example`
- Wire-generated code (`wire_gen.go`) manually verified consistent with `wire.go` injector
- Shutdown bug regression test (`shutdown_grace_bug_test.go`) confirmed: catches the early-return bug, verifies components always close
- Expired context test confirmed: validates non-expired deadline when grace > drain
- Integration tests properly guarded with `//go:build integration` build tags
- Migration concurrent safety: `markApplied()` duplicate-key handling verified in code
- No bare `return err` (all errors wrapped with context per GUARDRAILS.md)
- No `fmt.Println` or `log.Println` (Zap only)
- No naked goroutines
- No hardcoded secrets or sensitive data in logs
- README architecture claims verified against actual code

### Gaps found and fixed
1. **`.golangci.yml` migrated from v1 to v2 format** — v1 does not support Go 1.25; v2 required. `govet`, `errcheck`, `staticcheck` are default-enabled in v2 so they no longer appear in the explicit `enable` list. The v1 `run.timeout: 5m` was dropped (v2 has no timeout by default). Added exclusion for `pkg/errors/` revive var-naming false positive (intentional stdlib name shadowing).
2. **`.env.example` missing 2 env vars** — Added `ZEE6DO_MONGODB_CONNECT_TIMEOUT` and `ZEE6DO_MONGODB_SERVER_SELECTION_TIMEOUT` to match config bindings.
3. **`migrations/migrations.go` bare error returns** — Added `fmt.Errorf` wrapping to index creation errors per GUARDRAILS.md error handling rules.
4. **`README.md` inaccurate logging tool reference** — Changed "zerolog or zap" to "Uber Zap" (project uses Zap exclusively).
5. **`README.md` repository structure showed non-existent dirs** — Added `*` markers for planned-but-not-yet-created directories and a note distinguishing scaffolded vs planned.
6. **Missing test for port > 65535** — Added `TestLoad_panics_on_port_above_65535` to `pkg/config/config_test.go`.
7. **Test count inaccuracies** — Corrected from "32 unit tests, 8 integration tests" to "34 unit tests, 5 integration test functions".

## Milestone 5: Proto Definitions & Buf Setup
**Status:** Complete

### What was done
- Installed Buf CLI (v1.68.1), protoc-gen-go (v1.36.11), protoc-gen-go-grpc (v1.6.0)
- Created `buf.yaml` (v2 config, STANDARD lint rules, FILE breaking change detection, protovalidate dep)
- Created `buf.gen.yaml` (v2 config, local protoc-gen-go + protoc-gen-go-grpc plugins, source_relative paths)
- Created `buf.lock` via `buf dep update`
- Created `Makefile` with proto-gen, proto-lint, proto-breaking, build, test, lint targets

### Proto files (14 files in proto/zee6do/v1/)
All use `syntax = "proto3"`, `package zee6do.v1`, `option go_package = "github.com/DeepakDP5/zee6do-server/gen/zee6do/v1;zee6dov1"`.
Buf validate annotations on all required request fields.

- **common.proto** — Shared enums and messages:
  - Messages: `Pagination` (page_size with buf.validate gte:1/lte:100, page_token), `PaginatedResponse` (next_page_token, total_count), `User` (moved from auth_service), `NotificationSettings` (moved from user_service), `DateRange` (moved from analytics_service)
  - Enums: `SortOrder`, `TaskStatus` (TODO/IN_PROGRESS/BLOCKED/DONE/ARCHIVED), `TaskPriority` (P0-P3), `TaskSource` (MANUAL/VOICE/AI/SLACK/EMAIL/CLICKUP), `ConnectorType` (GOOGLE_CALENDAR/SLACK/EMAIL/CLICKUP)
  - Cross-reference comments on TaskSource/ConnectorType noting overlap with KnowledgeSourceType

- **auth_service.proto** — `AuthService`: SendOTP, VerifyOTP, SocialLogin, RefreshToken, ListDevices, RevokeDevice, Logout
  - Messages: SendOTP/VerifyOTP/SocialLogin/RefreshToken request/response pairs, Device (User moved to common.proto)
  - Enums: `SocialProvider` (GOOGLE/APPLE)
  - Validation: phone E.164 pattern `^\+[1-9]\d{6,14}$`, OTP code len=6, device_fingerprint required, provider not_in [0]

- **user_service.proto** — `UserService`: GetProfile, UpdateProfile, ExportData (server streaming), DeleteAccount
  - Messages: UserProfile, UserPreferences (NotificationSettings moved to common.proto)
  - Enums: `ThemePreference` (LIGHT/DARK)
  - Uses FieldMask for partial updates, imports common.proto (not auth_service.proto)

- **task_service.proto** — `TaskService`: CreateTask, GetTask, UpdateTask, DeleteTask, ListTasks, BatchUpdate, SearchTasks, CreateSubtask
  - Messages: Task (22 fields), RecurrenceRule, ConnectorReference, TaskFilter, all request/response pairs
  - Enums: `RecurrenceFrequency` (DAILY/WEEKLY/MONTHLY/YEARLY)
  - Imports common.proto enums (TaskStatus, TaskPriority, TaskSource, ConnectorType, SortOrder, Pagination)

- **ai_service.proto** — `AIService`: Chat (server streaming), ProcessNaturalLanguage, GetSuggestions, UndoAction, GenerateTemplate, GetDailyPlan
  - Messages: ChatResponse (with AIAction list), ParsedTask (with per-field confidence map), Suggestion (action_payload uses google.protobuf.Struct for type-safe JSON), DailyPlanItem
  - Enums: `AIActionType` (8 types), `SuggestionType` (5 types)

- **sync_service.proto** — `SyncService`: PullChanges, PushChanges (WatermelonDB protocol)
  - Messages: SyncCollectionChanges (created/updated/deleted bytes -- intentional for WatermelonDB protocol compat), SyncConflict
  - Enums: `ConflictResolution` (SERVER_WINS/CLIENT_WINS/AI_MERGED/MANUAL)

- **notification_service.proto** — `NotificationService`: GetPreferences, UpdatePreferences, RegisterDevice
  - Reuses `NotificationSettings` from common.proto (broke transitive dependency on user_service/auth_service)
  - Enums: `PushPlatform` (IOS/ANDROID)

- **analytics_service.proto** — `AnalyticsService`: GetMetrics, GetTimeBreakdown, GetAINarrative
  - Messages: DailyMetrics, CategoryTime, HourlyActivity (DateRange moved to common.proto)
  - Metrics: completion_rate, streak_days, tasks_created/completed/overdue, productive_hours

- **flag_service.proto** — `FlagService`: GetFlags
  - Messages: FeatureFlag (name, enabled, value)

- **connector_service.proto** — `ConnectorService`: LinkConnector, UnlinkConnector, SyncNow, GetSyncStatus, ListConnectors
  - Messages: ConnectorStatus, SyncResult
  - Enums: `ConnectionState` (CONNECTED/DISCONNECTED/SYNCING/ERROR)

- **storage_service.proto** — `StorageService`: GetUploadURL, GetDownloadURL, DeleteAttachment, ListAttachments
  - Messages: Attachment (id, task_id, filename, content_type, size_bytes, created_at)

- **billing_service.proto** — `BillingService`: VerifyReceipt, GetSubscriptionStatus, RestorePurchases
  - Messages: Subscription (status, plan, price, currency, dates)
  - Enums: `BillingPlatform` (APPLE/GOOGLE), `SubscriptionStatus` (ACTIVE/TRIAL/EXPIRED/GRACE_PERIOD/CANCELLED)

- **scheduler_service.proto** — `SchedulerService`: GetAgendaView, RescheduleTask, ResolveConflict
  - Messages: AgendaItem, CalendarEvent (uses DateRange from common.proto for GetAgendaViewRequest)
  - Enums: `AgendaItemType` (TASK/CALENDAR_EVENT), `ScheduleConflictResolution` (KEEP_TASK/RESCHEDULE_TASK/AI_DECIDE)

- **knowledge_service.proto** — `KnowledgeService`: AddDocument, SearchKnowledge, ListDocuments, DeleteDocument
  - Messages: KnowledgeDocument, KnowledgeSearchResult
  - Enums: `KnowledgeSourceType` (MANUAL/SLACK/EMAIL/CLICKUP/WEB)

### Generated Go code (gen/zee6do/v1/)
- 27 files: 14 `.pb.go` (message/enum types) + 13 `_grpc.pb.go` (gRPC server/client stubs)
- Package: `zee6dov1`
- Import path: `github.com/DeepakDP5/zee6do-server/gen/zee6do/v1`
- Generated code is committed (avoids requiring buf in CI)

### Dependencies added to go.mod
- `google.golang.org/grpc` v1.80.0
- `google.golang.org/protobuf` v1.36.11
- `buf.build/go/protovalidate` v1.1.3
- `buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go` (indirect, for validate annotations)
- `golang.org/x/sync` (upgraded)
- `google.golang.org/genproto/googleapis/api` + `googleapis/rpc` (indirect, gRPC runtime)

### Proto schema tests (internal/prototest/)
- 6 tests validating structural design decisions:
  - `TestUserInCommon` — User in common.proto usable across auth/user services
  - `TestNotificationSettingsInCommon` — NotificationSettings shared across user/notification services
  - `TestDateRangeInCommon` — DateRange reused by analytics + scheduler
  - `TestSuggestionActionPayloadIsStruct` — Struct type with marshal/unmarshal round-trip
  - `TestPaginationFieldExists` — Pagination field structure
  - `TestSendOTPRequestPhoneNumber` — Phone number field structure

### Verification
- `buf lint`: 0 warnings
- `buf generate`: 27 files generated (idempotent — regeneration produces identical output)
- `go build ./...`: clean
- `go test ./...`: all 38 tests pass (34 existing + 6 new proto schema tests in 7 packages)
- `golangci-lint run ./...`: 0 issues
- `make proto-breaking`: correctly skips on initial import (guard in Makefile)

---

## Milestone 5: gRPC Server Infrastructure (2026-04-16)

### What was added
- **pkg/errors/grpc.go** — `ToGRPCStatus(err)` maps all 10 sentinel errors to gRPC status codes; `ToGRPCError(err)` convenience wrapper
- **pkg/errors/errors.go** — Added `ErrRateLimited` and `ErrSubscriptionExpired` sentinels
- **pkg/middleware/** — 5 gRPC interceptors (unary + stream each):
  - `request_id.go` — UUID injection, reuses `x-request-id` from metadata
  - `logging.go` — request-scoped Zap logger with `request_id`, `rpc_method`; shared `logFields` holder lets downstream interceptors add fields (e.g. `user_id` from auth)
  - `recovery.go` — panic recovery, logs stack trace, returns `codes.Internal` (Sentry deferred)
  - `auth.go` — `JWTValidator` interface, bearer token extraction, skip-methods config, adds `user_id` to shared logFields
  - `validation.go` — buf protovalidate integration via `sync.Once` shared validator
  - `stream_wrapper.go` — shared `wrappedServerStream` type for context override
- **internal/grpc/server.go** — gRPC server with interceptor chain wired in order: request_id → logging → recovery → auth → validation. Registers `grpc_health_v1` health service. `SetHealthStatus(bool)` for load balancer integration. `Close(ctx)` with `GracefulStop` + context timeout fallback.
- **internal/grpc/wire.go** — Wire `ProviderSet`
- **pkg/config/config.go** — Added `JWTConfig` (Secret, AccessTTL, RefreshTTL) and `RateLimitingConfig` (Enabled, DefaultRPS) with defaults and explicit `BindEnv` entries. JWT secret required in non-development environments.
- **cmd/server/app.go** — `provideAuthConfig(cfg)` returns environment-aware auth: `placeholderValidator` (development) or `rejectAllValidator` (staging/production). Skip methods for auth endpoints.
- **cmd/server/wire_gen.go** — Manually updated (Wire CLI fails with Go 1.26.2)
- **cmd/server/main.go** — gRPC server started in goroutine with error channel
- **.env.example** — JWT and rate limiting env vars added

### Tests added
- `pkg/errors/grpc_test.go` — 15 tests: all 10 sentinel mappings, nil, wrapped, unknown, ToGRPCError
- `pkg/middleware/request_id_test.go` — 7 tests: UUID generation, reuse, empty handling, stream
- `pkg/middleware/logging_test.go` — 6 tests: field injection, completion log, error log, user_id propagation, stream
- `pkg/middleware/recovery_test.go` — 5 tests: no-panic passthrough, string/int panics, stream recovery
- `pkg/middleware/auth_test.go` — 13 tests: skip method, missing metadata, empty/invalid tokens, bearer stripping, stream auth
- `pkg/middleware/validation_test.go` — 8 tests: valid/invalid proto, non-proto, nil, constraints (SendOTPRequest), stream wrapper
- `pkg/config/config_test.go` — 4 new tests: JWT/rate-limiting defaults, JWT secret panic in non-dev, no panic in dev
- `internal/grpc/server_test.go` — 6 tests: server creation, start/stop lifecycle, unauthenticated RPC via health check, authenticated RPC with valid/invalid/missing tokens, forced-stop timeout, health status after close

### Known limitations
- Metrics and rate limiting interceptors deferred (not implemented)
- Recovery logs panics via Zap only; Sentry reporting deferred
- Auth validates JWT via interface; device fingerprint check deferred
- Health NOT_SERVING set only at `Close()` time, not during drain interval — LB may route during drain
- Interceptor chain order omits metrics + ratelimit (will be inserted when implemented)

### Verification
- `go build ./...`: clean
- `go test ./...`: all 60+ tests pass across 8 packages
- `golangci-lint run ./...`: 0 issues

---

## Ready for Next Session
- **To add auth module:** Create `internal/auth/` with model, repository, service, handler, wire. Implement real `JWTValidator` that parses JWT with `jwt.secret`. Register `AuthServiceServer` with gRPC server via `grpcServer.GRPCServer()`.
- **To add any new module:** Create `internal/<module>/`, define repository interface, add Wire provider set, register handler with gRPC server
- **To regenerate protos:** Run `make proto-gen` (or `buf generate`)
- **To add new proto service:** Create `proto/zee6do/v1/<name>_service.proto`, add `option go_package`, run `make proto-gen`, register handler
- **Wire caveat:** The `wire` CLI can fail with Go 1.25+. Write `wire_gen.go` manually when adding new providers (see Milestone 2 notes).
- **To add metrics interceptor:** Create `pkg/middleware/metrics.go`, insert after recovery in the chain in `internal/grpc/server.go`
- **To add rate limiting interceptor:** Create `pkg/middleware/ratelimit.go`, insert after metrics in the chain
- **To fix drain-interval health gap:** Have shutdown manager call `grpcServer.SetHealthStatus(false)` at step 1 alongside `HealthChecker.SetUnhealthy()`

---

## Verification: Tasks 1+2 (2026-04-21)
**Status:** Verified — no code gaps found

All beta checklist items for Sections 3.1–3.5, 1.3, 1.5 confirmed implemented against actual code:

- **14 proto files** in `proto/zee6do/v1/` — all 13 services + common types
- **27 generated Go files** in `gen/zee6do/v1/` — 14 `.pb.go` + 13 `_grpc.pb.go`
- **`pkg/middleware/`** — all 5 interceptors present: `request_id.go`, `logging.go`, `recovery.go`, `auth.go`, `validation.go` + `stream_wrapper.go`
- **`internal/grpc/server.go`** — interceptor chain order: `request_id → logging → recovery → auth → validation`
- **`pkg/errors/errors.go`** — all 10 sentinels: `ErrNotFound`, `ErrUnauthorized`, `ErrForbidden`, `ErrConflict`, `ErrAlreadyExists`, `ErrInvalidInput`, `ErrInternal`, `ErrUnavailable`, `ErrRateLimited`, `ErrSubscriptionExpired`
- **`pkg/errors/grpc.go`** — `ToGRPCStatus()` + `ToGRPCError()` mapping all 10 errors
- **`pkg/config/config.go`** — `JWTConfig` with `Secret`, `AccessTTL` (default 15m), `RefreshTTL` (default 30d)
- **Tests:** all packages pass, 0 failures

**No code changes made** — verification only. `pkg/crypto/` correctly deferred to Task 3 (Auth Module).

---

## Milestone / Task 3: Server Auth Module
**Status:** Complete
**Branch:** `vorflux/task-3-server-auth`

### pkg/crypto (JWT + device fingerprint + OTP)
- **`pkg/crypto/jwt.go`** — `JWTService` implements `middleware.JWTValidator`. HS256 signing with `cfg.JWT.Secret`; `GenerateAccessToken`, `GenerateRefreshToken`, `ValidateToken`, `ParseClaims`. Custom `Claims` struct carries `UserID` + `DeviceFingerprintHash` alongside `jwt.RegisteredClaims`.
- **`pkg/crypto/device.go`** — `HashFingerprint` (SHA-256 hex), `CompareFingerprint` (constant-time).
- **`pkg/crypto/otp.go`** — `GenerateOTP` (6-digit, `crypto/rand`, zero-padded), `HashOTP`/`VerifyOTP` (bcrypt cost 10).
- **Tests:** `jwt_test.go` (happy path, wrong secret, expired, garbage, DFH mismatch), `device_test.go` (deterministic + mismatch), `otp_test.go` (format + round-trip).

### internal/auth
- **`model.go`** — `Session` (raw DeviceFingerprint + RefreshTokenHash) and `OTPRecord` (phone, code hash, attempts, MaxAttempts=5, ExpiresAt, Verified).
- **`repository.go`** — `Repository` interface (OTP + sessions).
- **`mongo_repo.go`** — MongoDB implementation using `otp_records` and `sessions` collections (driver v2, `bson.ObjectID`).
- **`service.go`** — `Service` with `SendOTP`, `VerifyOTP`, `SocialLogin`, `RefreshToken`, `ListDevices`, `RevokeDevice`, `Logout`. Max 5 OTP attempts → `ErrRateLimited`; wrong code → `ErrInvalidInput` + attempts increment; refresh device-fingerprint mismatch → `ErrUnauthorized`. Refresh rotates (old session revoked). Social login is a beta stub — no real Google/Apple verification yet.
- **`handler.go`** — `zee6dov1.AuthServiceServer` implementation. All errors funneled through `apperrors.ToGRPCError`. Timestamps and proto conversions isolated in handler.
- **`wire.go`** — Wire provider set for auth module; binds `*crypto.JWTService` as `middleware.JWTValidator`.
- **Tests:** `service_test.go` (testify/mock across all methods, including rate-limit cross-over), `handler_test.go` (error→gRPC mapping for InvalidArgument, NotFound, Unauthenticated, PermissionDenied + happy paths), `integration_test.go` (build tag `integration`; full gRPC round-trip, auto-skipped when `ZEE6DO_TEST_MONGODB_URI` unset).

### internal/users (stub)
- **`model.go`** — `User` (Phone, Email, Name, SocialIDs map, timestamps).
- **`repository.go`** — `Repository` interface (`CreateUser`, `GetByID`, `GetByPhone`, `GetBySocialID`).
- **`mongo_repo.go`** — MongoDB implementation using `users` collection; duplicate-key → `ErrAlreadyExists`.
- **`wire.go`** — Provider set with `*mongo.Database` provider helper.

### migrations/002_auth_indexes.go
- `sessions`: index on `user_id`, compound `(user_id, revoked)`, TTL on `expires_at`.
- `otp_records`: TTL on `expires_at`, plain index on `phone_number`.
- `users`: unique on `phone`, unique+sparse on `email`.
- Registered in `migrations.Register`.

### cmd/server wiring
- `app.App` gains `AuthService *auth.Handler`; `newApp` registers it on the gRPC server via `zee6dov1.RegisterAuthServiceServer`.
- `provideAuthConfig` now takes `*crypto.JWTService` directly — no more `placeholderValidator` / `rejectAllValidator`. JWT validator is always real; dev/staging/prod differ only in the configured secret.
- `wire.go` + `wire_gen.go` updated **manually** (Wire CLI does not work on Go 1.26.2 per prior milestone notes).

### pkg/middleware
- Added exported `ContextWithUserID(ctx, userID)` helper to let the auth module's handler tests (and other packages) seed an authenticated user ID in context without running the interceptor.

### Dependencies added
- `github.com/golang-jwt/jwt/v5 v5.3.1`
- `github.com/stretchr/objx` (transitive, via testify/mock)
- `golang.org/x/crypto/bcrypt` (already a transitive dep; promoted via `go get`).

### Verification
- `go build ./...` → clean
- `go test ./... -count=1 -race` → all packages green
- `go test -tags integration ./... -run NoMatch` → compiles under the integration tag
- `go vet ./...` → clean
